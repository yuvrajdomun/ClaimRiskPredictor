# Lightweight requirements for Vercel deployment
# This file contains only essential dependencies to stay under 250MB limit

# Core dependencies
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
joblib==1.3.1

# Web framework
fastapi==0.100.0
uvicorn==0.22.0
pydantic==2.0.3

# Minimal ML dependencies (remove heavy ones)
# xgboost - REMOVED (large)
# lightgbm - REMOVED (large) 
# matplotlib - REMOVED (large)
# plotly - REMOVED (large)

# Keep only essential packages
pyyaml==6.0
python-multipart==0.0.6